<table class= "table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Symbol</th>
            <th>Current Price</th>
            <%# #working with @user instance variable %> 
            <% if @user.id == current_user.id %><%# if current user. we have to provide access to instance variable @user in the controller. using this partial to show
            users friends stocks as well, which is why this block exists- will prevent actions from rendering if not viewing own profile %>
            <th>Action</th>
            <% end %>
        </tr>
    </thead>
    <tbody>
        <% @user_stocks.each do |user_stock| %>
            <tr>
                <td><%= user_stock.name %></td>
                <td><%= user_stock.ticker %></td>
                <td><%= user_stock.last_price %></td>
                <%# if user is logged in and this is their own page %>
                <% if @user.id ==  current_user.id %>
                <td> <%= link_to "Delete this stock", user_stock_path(user_stock), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-xs btn-danger"%></td>
                <% end %>
            </tr>
        <% end %>
    </tbody>
</table>